import { apiSlice } from "../../Slices/apiSlice"
import { routes } from "../../../Routes/routes"


const extendedRootApi = apiSlice.injectEndpoints({
  endpoints: (builder) => ({
    postUpdateUserProfileTransfer: builder.mutation({
    query: (payload) => ({
        url: routes?.serverRoutes?.updateUserProfileTransferRoute,
        method: "POST",
        body: {payload},
      }),
      invalidatesTags: ["UsersData"]
    }),
  }),
  overrideExisting: false,
})

export const { usePostUpdateUserProfileTransferMutation } = extendedRootApi