import { apiSlice } from "../../Slices/apiSlice"
import { routes } from "../../../Routes/routes"


const extendedRootApi = apiSlice.injectEndpoints({
  endpoints: (builder) => ({
    postChangeAutoplayNext: builder.mutation({
    query: (payload) => ({
        url: routes?.serverRoutes?.changeAutoplayNextRoute,
        method: "POST",
        body: {payload},
      }),
      invalidatesTags: ["UsersData"]
    }),
  }),
  overrideExisting: false,
})

export const { usePostChangeAutoplayNextMutation } = extendedRootApi